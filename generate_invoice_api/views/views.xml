<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="generate_invoice_api.list">
      <field name="name">generate_invoice_api list</field>
      <field name="model">generate_invoice_api.generate_invoice_api</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="generate_invoice_api.action_window">
      <field name="name">generate_invoice_api window</field>
      <field name="res_model">generate_invoice_api.generate_invoice_api</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="generate_invoice_api.action_server">
      <field name="name">generate_invoice_api server</field>
      <field name="model_id" ref="model_generate_invoice_api_generate_invoice_api"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="generate_invoice_api" id="generate_invoice_api.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="generate_invoice_api.menu_1" parent="generate_invoice_api.menu_root"/>
    <menuitem name="Menu 2" id="generate_invoice_api.menu_2" parent="generate_invoice_api.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="generate_invoice_api.menu_1_list" parent="generate_invoice_api.menu_1"
              action="generate_invoice_api.action_window"/>
    <menuitem name="Server to list" id="generate_invoice_api" parent="generate_invoice_api.menu_2"
              action="generate_invoice_api.action_server"/>
-->
  </data>
</odoo>